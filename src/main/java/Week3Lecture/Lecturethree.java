package Week3Lecture;

import java.util.Scanner;

public class Lecturethree {
    public static void main (String[] args){
        Scanner reader = new Scanner(System.in);
        //not equals???
        // char smth = c.charAt(10);
        // c.lenght();
        // .substring(14); uzme od 14 indexa  do kraja ili .susbstring(2,5);
        /*
        b.indexOf("a"); trazi index ovog broja pvi put kad ga sretne i vraca ga
        b.indexOf("b"); ako ga nema, vraca -1 tj false

        String betweentwonum = b.substring(b.indexOf("a"), b.indexOf("b"));

        ArrayList<String> wordList = new ArrayList<String>(); <generics> --> it can take diff structurestj any type of object
        ArrayList<Student> students = new ArrayList<Student>(); ARRAY LIST DYNAMICALY ALLOCATES THE MEMORY WHILE LIST DOES NOT DYNAMICALLY ALLOCATE THE MEMORY

        .add() to add on arraylist
        .remove() to remove something from list
        .contains(true/false) vraca true false za nesto ako je u listi ili ne
        .get(0) daje sa indexa
        teachers.size();

        for-each

        for (String teacher : teachers){
            System.put.println(teacher);
        }

        collections.sort(teachers); alfabetski
        .shuffle
        .reverse
        create a method to return a boolean

        public enum Gender{
            male, female,
        }

        gender gender = Gender.MALE;
          */
    }
}
